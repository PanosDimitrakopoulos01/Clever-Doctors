public Patient search_Patient(String fullname){
		for(int i = 0;i < patients.length;i++)
		{
			if(patients[i].getFullname().equals==fullname){
                                System.out.print(patients[i]); 
				return patients[i];
		}
		return null;
	}
